import styled from "styled-components";

const Container = styled.div``;

const Organization = () => {
  return(
    <Container>
      Organization
    </Container>
  );
}

export default Organization;
